/**
 * Image size view
 */

'use strict';

// Deps
var Backbone = require( 'backbone' );

module.exports = Backbone.View.extend({
    template : '',
    events : {
    },
    bindings: {
    },
    initialize : function() {

    },
    render : function() {
        this.stickit();

        return this;
    }
});